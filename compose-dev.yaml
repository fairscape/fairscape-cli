services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: python:3.11.4 
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

